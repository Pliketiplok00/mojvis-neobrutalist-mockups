# MOJ VIS Backend Environment Configuration
# Copy this file to .env and adjust values as needed.

# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mojvis
DB_USER=postgres
DB_PASSWORD=postgres

# Or use a full connection string (takes precedence if set)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mojvis

# Admin Authentication (Phase 1b)
ADMIN_COOKIE_NAME=mojvis_admin_session
# Cookie domain: LEAVE EMPTY for localhost (host-only cookie). Set to ".mojvis.hr" in production.
ADMIN_COOKIE_DOMAIN=
# Cookie secure flag: false for localhost (HTTP), true for production (HTTPS)
ADMIN_COOKIE_SECURE=false
# Session TTL in hours (default 24)
ADMIN_SESSION_TTL_HOURS=24
# CORS allowed origin for admin panel (explicit, NOT wildcard)
# DEV: http://localhost:5173
# PROD: https://admin.mojvis.hr
ADMIN_ALLOWED_ORIGIN=http://localhost:5173

# Break-glass admin (emergency recovery only)
# REQUIRED: BREAKGLASS_MUNICIPALITY must be set if break-glass is enabled
# BREAKGLASS_USERNAME=breakglass
# BREAKGLASS_PASSWORD=your-secure-password-here
# BREAKGLASS_MUNICIPALITY=vis

# DeepL Translation API (optional - for admin HR→EN translation)
# Get free API key from https://www.deepl.com/pro-api
# DEEPL_API_KEY=your-deepl-api-key-here
